// -*- C++ -*-

#include <iostream>

#include <cmath>

int
main()
{

  std::cout << "std::floor(-0.5) = " << std::floor(-0.5) << "\n"
            << "std::floor(0.5) = " << std::floor(0.5) << "\n"
            << "std::floor(1.5) = " << std::floor(1.5) << "\n";

  return 0;
}
