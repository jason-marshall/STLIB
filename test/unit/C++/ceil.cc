// -*- C++ -*-

#include <iostream>

#include <cmath>

int
main()
{

  std::cout << "std::ceil(-0.5) = " << std::ceil(-0.5) << "\n"
            << "std::ceil(0.5) = " << std::ceil(0.5) << "\n"
            << "std::ceil(1.5) = " << std::ceil(1.5) << "\n";

  return 0;
}
